---
title: "Galactic VPC"
sidebarTitle: "Overview"
description: "Galactic VPC gives your project private IPv6 networks that span every Datum location your workloads run in."
---

Galactic VPC is Datum's private networking for your workloads. You create a *network* in your project, attach [Compute](/compute/overview) workloads to it, and each Instance receives a private IPv6 address on that network. One network spans every location that your workloads run in, so you don't create a separate network for each region.

<Note>
  Galactic VPC is in preview, and the `networking.datumapis.com/v1alpha` API can change.
</Note>

Galactic VPC runs on Datum's global IPv6 backbone. Datum plans the addresses, carries the traffic, and keeps each network separate from every other network.

## How it works

The following list describes how networks and Instances fit together:

- **Networks belong to a project:** You create networks with the `Network` resource. `datumctl compute deploy` creates a network named `default` for you the first time you deploy.
- **Every workload attaches to one network:** A workload names its network when you create it, and all of its Instances in every location attach to that network.
- **Addresses are private IPv6:** Each network gets its own private IPv6 prefix from the unique local address range. Each Instance gets an address from that prefix. The addresses aren't reachable from the internet.
- **Networks are isolated:** Instances on one network are separate from other networks in your project and from other customers' networks.
- **Public traffic arrives through a URL:** To serve a workload on the internet, you publish it on a Datum-managed HTTPS URL. Datum's edge forwards each request to an Instance over the private network. For more information, see [Publish a workload](/compute/publish-workloads).

## Features

Galactic VPC provides the following features:

- **One network across locations:** A network is a single project-wide resource. Every workload that names it, in any location, attaches to the same network, and each Instance gets a private IPv6 address on it.
- **Automatic addressing:** Datum assigns address space to each network and an address to each Instance. You don't plan subnets.
- **Stable addresses when you need them:** Instances keep their addresses when Datum replaces them, and a workload can keep addresses reserved when it scales down.
- **Declarative:** Networks are Kubernetes-style resources that you manage with `datumctl`, manifests, or GitOps tooling.

## What's next

<CardGroup cols={2}>
  <Card title="Networks" icon="network-wired" href="/galactic-vpc/networks">
    Create a network, attach a workload, and find Instance addresses.
  </Card>
  <Card title="Compute" icon="microchip" href="/compute/overview">
    Run workloads that attach to your networks.
  </Card>
</CardGroup>
